# venkatrepo
creating new repo
git clone <url>
ls
exmaple : creating new repo
cd venkatrepo
creating brach : git checkout -b <name>
git switch <branchname>
git pull {latest changes}
git branch -a {list of branches}

